<project 
	xmlns="http://maven.apache.org/POM/4.0.0" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
		http://maven.apache.org/POM/4.0.0 
		http://maven.apache.org/maven-v4_0_0.xsd">
		
	<modelVersion>4.0.0</modelVersion>
	
	<parent>
		<!-- which version of Jenkins is this plugin built against? -->
		<groupId>org.jenkins-ci.plugins</groupId>
		<artifactId>plugin</artifactId>
		<version>1.509.4</version>
	</parent>

	<groupId>me.automationdomination</groupId>
	<artifactId>threadfix-jenkins-plugin</artifactId>
	<version>1.0-SNAPSHOT</version>
	<packaging>hpi</packaging>

    <licenses>
        <license>
            <name>The MIT license</name>
            <url>https://github.com/automationdomination</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

	<developers>
		<developer>
			<id>automationdomination</id>
			<name>Brandon Spruth</name>
			<email>brandon@automationdomination.me</email>
		</developer>
		<developer>
			<name>Christopher Baek</name>
			<email>christopher.baek@gmail.com</email>
		</developer>
	</developers>
	
    <!--
    <scm>
        <connection>scm:git:ssh://github.com/automationdomination/threadfix-jenkins-plugin.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/automationdomination/threadfix-jenkins-plugin.git</developerConnection>
        <url>https://github.com/automationdomination/threadfix-jenkins-plugin</url>
    </scm>
    -->
    
    <!-- get every artifact through repo.jenkins-ci.org, which proxies all the artifacts that we need -->
    <repositories>
        <repository>
            <id>repo.jenkins-ci.org</id>
            <url>http://repo.jenkins-ci.org/public/</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>repo.jenkins-ci.org</id>
            <url>http://repo.jenkins-ci.org/public/</url>
        </pluginRepository>
    </pluginRepositories>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jersey.client.version>1.16</jersey.client.version>
        <joda.time.version>2.1</joda.time.version>
        <commons.validator.version>1.4.0</commons.validator.version>
        <!-- keep this just in case a code approach is used for   --> 
		<!-- uploading scans instead of the command line approach -->
		<!-- 
        <threadfix.cli.version>2.0FINAL-SNAPSHOT</threadfix.cli.version>
        -->
    </properties>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>1.7</source>
					<target>1.7</target>
				</configuration>
			</plugin>
		</plugins>
	</build>
	
    <dependencies>
        <dependency>
            <groupId>com.sun.jersey</groupId>
            <artifactId>jersey-client</artifactId>
            <version>${jersey.client.version}</version>
        </dependency>
        <dependency>
            <groupId>com.sun.jersey</groupId>
            <artifactId>jersey-json</artifactId>
            <version>${jersey.client.version}</version>
        </dependency>
        <dependency>
            <groupId>com.sun.jersey.contribs</groupId>
            <artifactId>jersey-apache-client</artifactId>
            <version>${jersey.client.version}</version>
        </dependency>
        <dependency>
            <groupId>joda-time</groupId>
            <artifactId>joda-time</artifactId>
            <version>${joda.time.version}</version>
        </dependency>
        <dependency>
			<groupId>commons-validator</groupId>
			<artifactId>commons-validator</artifactId>
			<version>${commons.validator.version}</version>
		</dependency>
		<!-- keep this just in case a code approach is used for   --> 
		<!-- uploading scans instead of the command line approach -->
        <!-- 
        <dependency>
        	<groupId>com.denimgroup.threadfix</groupId>
        	<artifactId>threadfix-cli</artifactId>
        	<version>${threadfix.cli.version}</version>
        	<exclusions>
        		<exclusion>
        			<groupId>org.jetbrains.annotations</groupId>
        			<artifactId>annotations</artifactId>
        		</exclusion>
        	</exclusions>
        </dependency>
        -->
    </dependencies>

</project>
