<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation=" http://maven.apache.org/POM/4.0.0    http://maven.apache.org/maven-v4_0_0.xsd">
		
	<modelVersion>4.0.0</modelVersion>
	<description>Uploads any supported ThreadFix scan file to your ThreadFix server</description>
	<url>http://wiki.jenkins-ci.org/display/JENKINS/Threadfix+Plugin</url>
	
	<parent>
		<!-- which version of Jenkins is this plugin built against? -->
		<groupId>org.jenkins-ci.plugins</groupId>
		<artifactId>plugin</artifactId>
		<version>1.509.4</version>
	</parent>

	<artifactId>threadfix</artifactId>
	<version>1.1-SNAPSHOT</version>
	<name>ThreadFix Plugin</name>
	<!-- which version of Jenkins is this plugin built against? -->
	<packaging>hpi</packaging>

    <licenses>
        <license>
            <name>The MIT license</name>
            <url>https://github.com/automationdomination</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

	<developers>
		<developer>
			<id>automationdomination</id>
			<name>Brandon Spruth</name>
			<email>brandon@automationdomination.me</email>
		</developer>
		<developer>
			<name>Christopher Baek</name>
			<email>christopher.baek@gmail.com</email>
		</developer>
	</developers>
	
    <!-- Added to publish on https://wiki.jenkins-ci.org/display/JENKINS/Plugins -->
    <scm>
      <connection>scm:git:ssh://github.com/jenkinsci/threadfix-plugin.git</connection>
      <developerConnection>scm:git:ssh://git@github.com/jenkinsci/threadfix-plugin.git</developerConnection>
      <url>https://github.com/jenkinsci/threadfix-plugin</url>
    </scm>

    <!-- get every artifact through repo.jenkins-ci.org, which proxies all the artifacts that we need -->
    <repositories>
        <repository>
            <id>repo.jenkins-ci.org</id>
            <url>http://repo.jenkins-ci.org/public/</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>repo.jenkins-ci.org</id>
            <url>http://repo.jenkins-ci.org/public/</url>
        </pluginRepository>
    </pluginRepositories>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
	<main.basedir>${project.basedir}</main.basedir>
        <jersey.client.version>1.16</jersey.client.version>
        <joda.time.version>2.1</joda.time.version>
        <commons.validator.version>1.4.0</commons.validator.version>
        <org.slf4j.version>1.7.7</org.slf4j.version>
        <threadfix.cli.version>2.0FINAL-SNAPSHOT</threadfix.cli.version>
    </properties>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>1.7</source>
					<target>1.7</target>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<inherited>false</inherited>
				<executions>
				  <execution>
					<id>install-threadfix-cli</id>
					<phase>validate</phase>
					<goals>
					  <goal>exec</goal>
					</goals>
				  </execution>
				</executions>
				 <configuration>
				  <executable>mvn</executable>
				    <arguments>
					<argument>install:install-file</argument>
					<argument>-Dfile=${project.basedir}/lib/threadfix-cli-${threadfix.cli.version}-jar-with-dependencies.jar</argument>
					<argument>-DgroupId=com.denimgroup.threadfix</argument>
					<argument>-DartifactId=threadfix-cli</argument>
					<argument>-Dversion=${threadfix.cli.version}</argument>
					<argument>-Dpackaging=jar</argument>
				  </arguments>
				 </configuration>
			</plugin>
		</plugins>
	</build>

    <dependencies>
        <dependency>
            <groupId>com.sun.jersey</groupId>
            <artifactId>jersey-client</artifactId>
            <version>${jersey.client.version}</version>
        </dependency>
        <dependency>
            <groupId>com.sun.jersey</groupId>
            <artifactId>jersey-json</artifactId>
            <version>${jersey.client.version}</version>
        </dependency>
        <dependency>
            <groupId>com.sun.jersey.contribs</groupId>
            <artifactId>jersey-apache-client</artifactId>
            <version>${jersey.client.version}</version>
        </dependency>
        <dependency>
            <groupId>joda-time</groupId>
            <artifactId>joda-time</artifactId>
            <version>${joda.time.version}</version>
        </dependency>
        <dependency>
	    <groupId>commons-validator</groupId>
	    <artifactId>commons-validator</artifactId>
	    <version>${commons.validator.version}</version>
	</dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-log4j12</artifactId>
            <version>${org.slf4j.version}</version>
        </dependency>
	<dependency>
	    <groupId>org.codehaus.mojo</groupId>
	    <artifactId>animal-sniffer-annotations</artifactId>
	    <version>1.9</version>
	</dependency>
        <dependency>
            <groupId>com.denimgroup.threadfix</groupId>
            <artifactId>threadfix-cli</artifactId>
            <version>2.0FINAL-SNAPSHOT</version>
	    <scope>system</scope>
	    <systemPath>${project.basedir}/lib/threadfix-cli-${threadfix.cli.version}-jar-with-dependencies.jar</systemPath>
        </dependency>
    </dependencies>
</project>
